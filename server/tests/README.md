# Nasty test suites (server-authoritative online rebuild, v0.15 onward)

Every script here launches its OWN private server instance (random port, scratch
`NASTY_ROOMS_DIR`/`NASTY_KV_PATH`, throwaway admin token) - none of them ever touch the
production LaunchAgent, the real rooms dir, `server/leaderboard.json`, or the default KV.
Playwright is loaded from `/Users/jarvis/clawd/node_modules/playwright` (this machine's
existing install, same as every prior release's test tooling).

Run everything from this directory with plain `node <script>`; scripts that support the Deno
server take `SERVER=deno` as an env var, or `deno` as their first argument (per-suite, see the
"How to run" column).

### Harness gotcha: a raw WebSocket client MUST answer `ping`

If you write a test that talks raw WebSocket instead of driving a browser page, it has to echo
the server's app-level `{type:'ping'}` back as `{type:'pong'}`, the way `index.html` does. Both
servers force-close any socket that goes 12 seconds without a single inbound application
message (`SOCKET_STALE_MS`, see § HEARTBEAT in `server.js` and `cloud/server.ts`). A harness
that stays silent gets torn down mid-game, which shows up as a mysterious stall rather than an
error. Copy the two-line `ws.on("message", ...)` echo from `smoke_server.js` or
`sim_paused_room_gate.js`.

This bit **on 2026-07-26**: it used to be a Deno-only hazard on a 12s window while `server.js`
ran a far more forgiving 25-50s protocol-frame rule, so a silent harness could get away with it
against Node. Both servers now use the same rule and the same window.

**To run the whole bar in one command**, from `server/`:

```
npm run test:all           # every suite below, in order, with a pass/fail matrix at the end
npm run test:all:quick     # same, minus the long soaks and audits
node tests/run-all.js --list          # print the plan without running anything
node tests/run-all.js --server=deno   # point the dual-mode suites at the Deno server
node tests/run-all.js --only=leaderboard,surrender
```

`npm test` (unchanged) is the fast 4-suite pre-commit check: the two engine checks plus
`test_own_landing_illegal.js` and `test_deck_conservation.js`. `npm run test:all` is the real
bar. Each suite gets its own time cap inside `run-all.js` (macOS has no `timeout` command, so
the cap is implemented in node); a suite that blows its cap is reported as `STALL`, which is a
distinct outcome from `FAIL` so an environment hiccup is not mistaken for a regression.

| Script | What it proves | How to run |
|---|---|---|
| `../test-engine-sync.js` | generated engine files match index.html (run via `npm test` in `server/`) | `cd .. && npm test` |
| `../test-engine-headless.js` | full CPU-vs-CPU games straight against the engine module | (same `npm test`) |
| `smoke_server.js` | raw wire-protocol basics vs the Node server (protocol handshake, unattended all-CPU game, illegal-move rejection, tableSpeed) | `node smoke_server.js` |
| `smoke_deno.js` | same vs the Deno server + KV snapshot-size check | `node smoke_deno.js` |
| `restart_deno.js` | SIGKILL the Deno server mid-game, restart on the same KV path, game continues from the KV snapshot | `node restart_deno.js` |
| `protocol_checklist.js` | the FULL protocol surface, 53 checks as of v0.23.1 (version handshake incl. protocol 1/2/3 lockouts, lobby flows, reclaim incl. contested approve/deny, pause, presence, nudge, leaderboard/solo-result/epoch, admin god-mode, CORS, rate limit, AASA, /join redirect) | `node protocol_checklist.js node` / `node protocol_checklist.js deno` |
| `chaos_v15.js` | Playwright chaos scenarios: `full` (clean game), `hostbg` (THE host-background repro of Blake's bug), `chaos N` (N full games with realistic random background/reconnect cycles + convergence after every cycle) | `node chaos_v15.js full` / `hostbg` / `chaos 3`; prefix `SERVER=deno` for the Deno server |
| `reconnect_storm.js` | the v0.7.4/v0.9-era kick-harness recipe on the new architecture: 4 human seats, rotate dropping/reconnecting one per cycle mid-play, convergence after every cycle | `node reconnect_storm.js 18` / `SERVER=deno node reconnect_storm.js 18` |
| `test_leaderboard_scenarios.js` | leaderboard exactly-once: solo w/ server reachable, solo w/ server down then queue drain, online game server-side recording + reconnect-no-double | `node test_leaderboard_scenarios.js` |
| `test_leaderboard_split.js` | v0.21 solo/teams point split: new-style client writes hptsS/hptsT directly, a legacy (pre-split) client's plain hpts delta gets attributed to the right bucket (plus a non-win and an orphan-hpts edge case), and the boot/startup migration derives split keys from pre-existing legacy-only data (unambiguous + genuinely-ambiguous wins-ratio cases, mirrors real production shape) - idempotent across a second boot | `node test_leaderboard_split.js node` / `node test_leaderboard_split.js deno` |
| `test_leaderboard_ui_split.js` | v0.21 leaderboard Solo/Teams tab UI: tab math + exact empty-state copy for each tab (direct renderLb()/setLbTab() calls), the v0.19.1 fixed-layout table still fits at 320px with zero wrapped cells on BOTH tabs, and an end-to-end pass against a real global board (default tab is Solo, switching tabs filters correctly, reopening resets to Solo, admin panel's split-points editor unlock/edit/save reflected back in the tabs) | `node test_leaderboard_ui_split.js` |
| `soak_offline.js` | the standing offline soak recipe (`#autotest` 4P / `#autotest6` 6P), bit-identical-offline regression check | `node soak_offline.js both` |
| `test_v16_features.js` | v0.16 items 2/4/6: ready-up gate, "Leave for good" CPU takeover + leaderboard exclusion, non-host pauseToggle reaching everyone | `node test_v16_features.js node` / `node test_v16_features.js deno` |
| `test_push_notifications.js` | v0.16 item 5: `registerPush` accepted + persisted, no push while connected, exactly-once would-send-push log on disconnect (with the right token/name), no push for a seat with no registered token | `node test_push_notifications.js node` / `node test_push_notifications.js deno` |
| `test_recalibration.js` | v0.20: the "resync" wire message (live-connection fresh snapshot, no presence ripple, silently ignored if never identified); the ACTUAL root-cause reproduction (silent background-freeze drift + genuine table idle, no self-heal without this fix); input lock during recalibration; the failure path + tap-to-reset recovery; killing the server mid-recalibration then restarting and recovering via Reset connection; a pre-v0.20 client (never sends "resync") still playing normally against the new server | `node test_recalibration.js node` / `node test_recalibration.js deno` |
| `test_reconnect_retry.js` | 2026-07-23 item 1: a single failed tile-tap reconnect attempt retries automatically (with visible "Reconnecting..." feedback, never a silent dead end) instead of dumping the player onto the manual join screen; kills the server, taps the tile, restarts the server, proves recovery with zero manual action; the last-resort join-screen fallback skips the offline one-time speed picker (item 3/4's confusion) while a genuinely new join still shows it | `node test_reconnect_retry.js node` / `node test_reconnect_retry.js deno` |
| `test_reunion_readyup.js` | 2026-07-23 item 2: the reunion ready-up gate - opens for EVERYONE at the table (not just the returnee), presence alone never auto-resumes, resumes automatically once every connected human has readied, idempotent re-requests, a disconnected seat never blocks it, `takeOverSeat`/a disconnect can each complete an open gate, guard checks, and the gate's state (open + who's already readied) survives a real server restart | `node test_reunion_readyup.js node` / `node test_reunion_readyup.js deno` |
| `test_table_speed_lock.js` | 2026-07-23 items 3+4: the host's chosen speed rides the first deal and survives a real server restart identically for every player, the host can change it again at any time (including post-restart) and a guest never can, and - the actual bug this suite caught - a real-browser check that the ACTUAL animation-pacing `SPEED` variable (not just the `NET.tableSpeed` data field) is correctly synced on both a fresh deal and a reconnect, regardless of either player's own local offline preference | `node test_table_speed_lock.js node` / `node test_table_speed_lock.js deno` |
| `test_topbar_buttons.js` | 2026-07-23 items 5+12: every topbar button (Quit/Pause/Save/Speed/Rules/Mute) is plain mixed-case text with no emoji, rendered all-caps via CSS, uniform height at every width from 320px up to the widest current iPhone (Skip explicitly excluded - it lives outside `#topbar`); the new Save button saves IMMEDIATELY (before the confirm is even answered), never pauses the table, Cancel keeps playing with zero consequence, confirming reuses the exact "Save & leave" path (no stat/loss recorded, save survives, a real "Resume Saved Game" tile appears); Pause is byte-identical to the old combined button (still pauses, still every original sheet option) | `node test_topbar_buttons.js` (file://, offline only) |
| `test_overlay_sizing.js` | 2026-07-23 item 6: `.confirmCard` (pause/leave/concede/overwrite/slot-replace) never exceeds the viewport at 320x568 through 430x932 - critically, using REAL iPhone safe-area insets via CDP `Emulation.setSafeAreaInsetsOverride` (notch/Dynamic-Island top strip + home-indicator bottom strip), the exact dimension both the v0.27.1 and iOS-build-38 fix attempts missed; a small 2-button dialog never stretches to fill the whole screen; a pathological viewport too short for any sizing scheme still lets every button be reached via internal scroll (never silently unreachable); a viewport resize AFTER the dialog is already open still fits (proves the fix is reactive CSS, not the old one-shot JS `transform:scale()`) | `node test_overlay_sizing.js` (file://, offline only) |
| `test_stable_bubbles.js` | 2026-07-23 item 8: a SOLO pickable stable (base) tee's tap bubble is immune to unrelated board congestion (position byte-identical whether or not another tee is simulated sitting exactly where it would land - the old bug fell through to the board-wide-avoidance sweep and could push it far away), stays close to its own tee, the full 5-tee stable formation is still symmetric/non-overlapping/on-screen at 4P+6P and 320/390px, a solo bubble is still a correctly-routed real tap target, and crowded TRACK bubbles keep their existing v0.23 sideways-dodge behavior completely untouched | `node test_stable_bubbles.js` (file://, offline only) |
| `test_card_linger.js` | 2026-07-23 item 11: exactly one "last played" card visible at a time and it visibly survives at least one `G.turn` change (not just showing during the play itself), a fresh deal (`fadeAllDiscards()`) still clears every lingering card, `showDiscard()` replaces rather than stacking, the hand-card and table-card rank text is measurably larger (>=14px / >=19px vs the old 13px/17px) with the two-character "10" never overflowing its own card at 320px width while the card's own size is unchanged, and a smoke check that a lingering card is always real public already-played info, never a hidden-hand peek | `node test_card_linger.js` (file://, offline only) |
| `test_jack_swap_index.js` | 2026-07-23 item 13 (THE JACK BUG) - reproduce-then-fix: plays a real game against the actual running server (real CPU seats + one real human seat driven by the test), keeps a shadow copy of the rules engine in lockstep purely from the broadcast action stream, and the moment the tracked seat holds a Jack with 2+ of its own tees on the track, deliberately submits a swap using the NON-lowest-indexed eligible piece (exactly what a real second Jack-able tee tap would send) and asserts the server's echoed move uses that exact piece, not the lowest-indexed one `sameMove()`'s missing `pi` check used to silently substitute | `node test_jack_swap_index.js node` / `node test_jack_swap_index.js deno` |
| `test_dealer_random.js` | 2026-07-23 item 10 - verifies (rather than assumes) that a brand-new game's first dealer is genuinely randomized across every seat: offline `newGame()` via `engine.js` (4000 trials at 4P, 3000 at 6P teams) and online host `actuallyStartGame()` against BOTH real running servers (240 fresh rooms each at 4P/6P, reading the dealer off the real `start` gameAction) - asserts every seat appears and no seat's share deviates more than 50% from uniform | `node test_dealer_random.js` (offline only) / `node test_dealer_random.js node` / `node test_dealer_random.js deno` / `node test_dealer_random.js all` |
| `test_cpu_kick_fairness_audit.js` | 2026-07-23 item 7 - quantifies "does the CPU always have the perfect card": an all-CPU-hard baseline with no human seat proves kicks land on every seat roughly evenly (no seat-identity targeting), then a real human-in-the-seat soak (two skill levels, 150 games each, worst-case 3x Nasty-difficulty opponents) reports actual kicks-per-game, CPU kick-take-rate when available, longest same-victim kick streak, and win rate - honest numbers, not a pass/fail proxy | `node test_cpu_kick_fairness_audit.js` (offline only) |
| `test_knockout_leaderboard.js` | Item 9 (2026-07-24) - the 3rd leaderboard tab, KOs: Part A/B (client-only, no server) prove the tab's rendering/sorting/divide-by-zero math and the REAL `tallyKnockout()` function directly (human-on-human credits both sides, CPU-involved credits only the human side, a forced partner-kick is excluded before any bookkeeping, migration-safe on a partial/missing G); Part C (server e2e) plays real FFA games and compares the server's own recorded `hkoDealt`/`hkoTaken` EXACTLY against the test's independently-computed expected totals; Part D proves migration safety against a pre-existing legacy entry; Part E (best-effort, live) hunts for a genuine forced partner-kick and confirms it's excluded server-side too | `node test_knockout_leaderboard.js` (client-only) / `node test_knockout_leaderboard.js node` / `node test_knockout_leaderboard.js deno` / `node test_knockout_leaderboard.js all` |
| `test_online_teams.js` | Item 14 (2026-07-24) - choosing a Teams game online, entirely through real UI clicks: the host-speed screen's new Players/Game-type toggle + live partner-pairing preview; the created room's `lobby.teams`; both the host's room screen and a guest's join-seat screen showing the same mode + real-name pairing; the dealt game genuinely in team mode with the correct pairing on both the host's and guest's independent `G`; a reunion mid-game showing the pairing note; an untouched host flow still producing plain FFA (regression); 6-player teams (3 pairs) | `node test_online_teams.js node` / `node test_online_teams.js deno` |
| `test_online_cpu_difficulty.js` | 2026-07-24 follow-up ("let me set the CPU difficulty for online games"): the wire already carried per-seat `diff` end to end - the gap was purely a missing lobby control. Proves it through the REAL UI: the host changes a CPU seat's tier (Easy/Tricky/Nasty) from the room screen; every guest sees the current tier read-only on their seat screen; a non-host has no control AND the server independently refuses a `setSeat {patch:{diff}}` sent directly; the started game's real `G.seats` carry the chosen tier (the CPU genuinely plays at it, not just a label); a garbage diff value is rejected rather than stored verbatim | `node test_online_cpu_difficulty.js node` / `node test_online_cpu_difficulty.js deno` |
| `test_postgame_review.js` | 2026-07-25, Blake's "post-game review mode" ask: Part A - the NEW `G.koDealtAll`/`koTakenAll` pair credits EVERY seat (human or CPU) while the existing human-only `koDealt`/`koTaken` pair that feeds the lifetime leaderboard is untouched, forced partner-kicks still excluded from both; Part B - a REAL naturally-finished offline game where the win popup's KO table matches those counters exactly for every seat, no Infinity/NaN, "Perfect" wording used correctly; Part C - X closes to the board in review mode, no hand revealed by default, tapping a plaque reveals exactly that seat's real cards and tapping again hides it, topbar repurposed correctly, "Results" reopens the same popup with the reveal state intact; Part D - the reveal is unreachable while a game is still in progress; Part E - leaving from post-game returns to the menu | `node test_postgame_review.js` |
| `test_ai_difficulty.js` | The AI tier ladder, a permanent measurement rather than a proxy: Nasty still beats Easy and Easy still beats random; Tricky-vs-Easy is unchanged from the v0.17 baseline (proves the v0.18 Nasty-only sharpening really did leave the other tiers alone); new Nasty vs a frozen verbatim copy of the v0.17 Nasty policy; Nasty vs Tricky; all-Nasty games still finish (no stalling from the deeper rollout search); and `chooseAI()` stays inside the ~50ms/decision budget that the same code has to meet on phones AND on the cloud server. Also guards the v0.21 fairness fix (`cloneG()` no longer hands the rollout the real hidden hands/deck) | `node test_ai_difficulty.js` (offline only, SLOW) |
| `test_deck_conservation.js` | Card conservation (fairness audit, 2026-07-18): at EVERY checkpoint of a real simulated game - after `newGame()`, after every deal, after every applied move, after every pass/bow-out - `deck + all hands + discard === 52`, exactly, regardless of player count, teams mode, dealer, or how many reshuffles happen. A violation means a card was duplicated or lost in the bookkeeping. Mixes all three AI tiers so every `chooseAI()` path (including the hard tier's rollout, which simulates whole hands inside a cloned `G`) is exercised | `node test_deck_conservation.js` (wired into `npm test`) |
| `test_own_landing_illegal.js` | v0.23 rules unit check: landing on your OWN peg is illegal - never offered, never forced. Plus v0.23.1 (Blake's confirmed ruling): landing on a PARTNER's peg is a LAST RESORT - excluded whenever any other legal move exists anywhere in the hand, offered (and so effectively forced) when it is the only possible play, and then it kicks the partner's peg back to base instead of the player bowing out. Asserts the whole matrix | `node test_own_landing_illegal.js` (wired into `npm test`) |
| `test_seat_gate.js` | v0.22 P0b - the regression test for Blake's "we only had four cards in our initial deal": the server used to deal hand 1 the instant the host started, while the humans were still reading the pre-game popups, so their turns arrived with no legal move and the CPUs played the whole hand. Proves the first deal now waits until every player who promised a `{type:'seated'}` signal has sent it, with a server-side cap and early release on disconnect, and that old clients (which never promise) get the exact pre-v0.22 behavior | `node test_seat_gate.js node` / `node test_seat_gate.js deno` |
| `test_freeze_recovery.js` | v0.22 Layer 1, the network-layer freeze harness: a real silent freeze via `freeze_proxy.js` COMBINED with a real JS-runtime freeze (CDP `Page.setWebLifecycleState`), i.e. timers and reconnect logic genuinely stop, exactly like a backgrounded iOS webview. Proves: a frozen HOST never blocks anyone else (the P0 regression - the old blocking overlay is gone and must stay gone); the server-driven away ladder (`nudged` -> `cpuOffer` -> any other player can make the server play that one turn, seat stays human) and how it composes with pause; convergence after unfreeze; the zombie-socket shape self-healing with zero manual taps; and the old-build protocol lockout for the exact pinned build 28 (protocol 2) and build 30 (protocol 3) clients | `node test_freeze_recovery.js node` / `node test_freeze_recovery.js deno` (SLOW - 60s freeze; `NASTY_TEST_FREEZE_MS` shortens it) |
| `test_menu_bubble_race.js` | v0.26 - the "frozen update bubble follows you to the menu" bug Blake kept hitting. Leaving to the menu never bumped `NETGEN`, so an in-flight turn loop or a resolving kick/swap animation could write a fresh toast AFTER the menu was already showing and then sit there forever with `NET` torn down. Exercises both layers of the fix (NETGEN++ on every leave path, plus `turnPrompt()`/`handleAwayStatus()` refusing to touch `#toasts` unless the game screen is genuinely showing), as three soak scenarios against ONE long-lived room (never re-hosting, so the host-create rate limiter is never hit) | `node test_menu_bubble_race.js node` / `node test_menu_bubble_race.js deno` (SLOW - 60-rep soak) |
| `test_spotlight_v024.js` | v0.24 item 3 - SPOTLIGHT tuning + glow. The boost dropped from a flat 2x to two tiers (1.2x when a HUMAN seat's peg is involved, the original 2x otherwise), and a pulsing gold glow marks the involved tee(s) for a human-involved skip kick/swap (the victim keeps glowing through an extra stable beat). Proves all of it is SKIP-only and human-only: normal play and INSTANT reconnect catch-up are untouched, and a pure CPU-vs-CPU skip kick keeps the old plain 2x pace with no glow | `node test_spotlight_v024.js` (file://, offline only) |
| `test_ui_v024.js` | v0.24 items 1+2 - the rules panel's dealing text at both game sizes, and the seat-list rename affordance in both the offline menu and the online lobby. Parts A/B are offline (file://); Part C spins up its own private `server.js` instance for the lobby half | `node test_ui_v024.js` |
| `test_surrender.js` | v0.27 - SURRENDER. Blake's ask was "replace the Menu button with a Quit button that warns it counts as a loss", widened in-session to cover EVERY action that permanently ends an unfinished game: the topbar Quit button, "Leave without saving", "Have a computer take over my seat", the saved-tile trash delete, and the slot-replace chooser discarding a slot. Only "Save & leave"/"Return to Game"/Cancel stay consequence-free. Also covers v0.27.1's "surrender"->"concede" display rename (internals and the `{type:'surrender'}` wire message are unchanged) and the NO-FAULT EXIT rule: once any human has conceded an unfinished ONLINE game, every other human's later departure from that same game is free | `node test_surrender.js node` / `node test_surrender.js deno` (SLOW) |
| `test_v025_ui_flows.js` | v0.25 - the acceptance-bar UI flows nothing else drove through the REAL DOM: the invitee flow (rules popup before seat selection, claim a seat, "Ready up" lighting the host's Start); the host's pre-room speed picker being what a guest actually runs at; Blake's exact regression (two humans quit via "Save & leave", one relaunches to the MENU - NOT auto-rejoined - taps the saved tile, gets the rejoin lobby, converts the absent human to a CPU, resumes); the counterpart where the other human never left and the tile goes straight to the board; the single-screen Pause/Save sheet; a disconnected player's name rendering red; and v0.27's topbar Quit confirmation | `node test_v025_ui_flows.js node` / `node test_v025_ui_flows.js deno` (SLOW) |
| `sim_paused_room_gate.js` | Real WebKit in the real iOS Simulator (simctl): a phone holding a perfectly valid stored session for a room that is currently PAUSED must NOT silently sit down at that table when it is cold-launched - the room stays paused and untouched and the player stays disconnected until somebody deliberately taps the resume tile. Finishes with a control step (a raw socket rejoins with the very same credentials) so a pass proves the phone's abstention was deliberate rather than a broken token. **Was `sim_lifecycle.js`**; its Legs 1-3 asserted SILENT auto-rejoin on cold launch, which was deliberately REMOVED in v0.25 - they were deleted on 2026-07-25 rather than left asserting reverted behavior. Do not write them back | `node sim_paused_room_gate.js node` / `node sim_paused_room_gate.js deno` (SKIPs cleanly with no Simulator) |
| `test_reunion_gate_hardening.js` | 2026-07-25 - the companion to `test_reunion_readyup.js` (which proves the gate's HAPPY path); this one covers only the ways the reunion ready-up gate could be bypassed, wedged, or opened by the wrong person. Three confirmed server-side bugs, reproduced first and then proved fixed: an unpause used to bypass the open gate AND leave it stuck open forever (killing every later reunion in that room); a connected player who never tapped "Ready up" could wedge the table indefinitely, with the away ladder's CPU-takeover escape disabled the whole time (now capped, like the pre-start seat gate); and the gate could be opened by the wrong player | `node test_reunion_gate_hardening.js node` / `node test_reunion_gate_hardening.js deno` |
| `test_leaderboard_names_and_deltas.js` | 2026-07-25 - two confirmed server-side leaderboard bugs, reproduced first and proved fixed identically on BOTH servers. (1) Stat deltas were only checked for `Number.isFinite`, so a negative delta was accepted and applied on Node (a lifetime stat could go DOWN) while the same delta threw on Deno, and a mixed negative-then-positive delta aborted Deno's write loop mid-way, silently losing the sibling positive key and never marking the game seen - so a client's offline queue would retry that game forever. Now the whole submission is validated before anything is written, and the seen marker is written first. (2) The board treated "Blake", "blake" and "BLAKE" as three different people (very likely in practice: the same person on a phone vs an iPad). The board is now keyed case-insensitively, keeping the display capitalization already on it | `node test_leaderboard_names_and_deltas.js node` / `node test_leaderboard_names_and_deltas.js deno` |
| `freeze_proxy.js` | **HELPER, NOT A SUITE - do not run it directly.** A tiny TCP proxy that can silently stop forwarding while keeping every connection open, reproducing the iOS/WKWebView zombie-socket shape (WebKit bug 228296 family) at the network layer with the client code path 100% untouched. Imported by `test_freeze_recovery.js` | (not run on its own) |
| `run-all.js` | **RUNNER, NOT A SUITE.** Runs every suite in this table in order with a per-suite time cap and prints a pass/fail/stall/skip matrix. See the top of this file | `npm run test:all` (from `server/`) |

Expected results as of v0.15 (2026-07-16) are recorded in HANDOFF.md's v0.15 section - if a
run here diverges from those numbers, treat it as a regression until proven otherwise. The
2026-07-23 batch's own expected numbers are recorded in HANDOFF.md's
"(pending) Blake's 2026-07-23 list, items 1-4" section; the items 5/6/8/11/12 batch's numbers
are in that same pending area's own subsection; the items 7/10/13 batch's numbers are in that
same pending area's own "Items 7, 10, 13" subsection; items 9/14's numbers are in that same
pending area's own "Item 9"/"Item 14" subsections.

Note: `test_topbar_buttons.js`, `test_overlay_sizing.js`, `test_stable_bubbles.js`,
`test_card_linger.js`, `test_spotlight_v024.js`, and `test_postgame_review.js` are pure
client-side UI regressions (file://, no server involved at all) - unlike most suites in this
table, they don't spin up a private server instance, because nothing they cover touches the
network protocol or either server. (`test_ui_v024.js` is a hybrid: Parts A/B offline, Part C
against its own private server.)

Known slow / known flaky, for when you are reading a `run-all.js` matrix:

- **Slow by design** (skipped by `npm run test:all:quick`): `test_ai_difficulty.js` and
  `test_cpu_kick_fairness_audit.js` (hundreds of full games each), `soak_offline.js`,
  `reconnect_storm.js`, `chaos_v15.js`, `test_menu_bubble_race.js` (60-rep soak),
  `test_freeze_recovery.js` (a real 60s freeze), `test_surrender.js`, `test_v025_ui_flows.js`,
  `test_dealer_random.js`.
- **Flaky**: `test_knockout_leaderboard.js`'s live 2-human WebSocket leg (Part E) depends on a
  genuinely rare in-game event arising inside a bounded budget and can stall. `run-all.js`
  therefore runs that suite in its client-only mode; run it in `all` mode by hand when you are
  specifically working on knockouts.
- **Environment-dependent**: `sim_paused_room_gate.js` needs an iOS Simulator and reports SKIP
  without one. `smoke_deno.js` / `restart_deno.js` and any suite invoked with `deno` need the
  `deno` binary on PATH.
