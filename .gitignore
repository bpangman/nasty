HANDOFF.md
PLANNING.md
RULES.md
reference/
server/node_modules/
server/rooms/
server/admin-token.txt
server/leaderboard.json
server/leaderboard-epoch.json
server/solo-ids.json
*.log

# --- iOS app wrapper (app/) — see app/APPSTORE.md ---
app/node_modules/
# app/www is generated by `npm run build:www` from the root game files — never hand-edit
app/www/
app/ios/DerivedData/
app/ios/build/
app/ios/App/build/
app/ios/App/Pods/
# Capacitor's own copy of www, regenerated by `cap sync`
app/ios/App/App/public/
server/deno-deploy-token.txt
server/deno-deploy-org-token.txt
server/AuthKey_*.p8
server/apns-key.p8
server/apns-key-id.txt
server/ci-keychain-pass.txt
server/ci-dist-key.pem
.beta_watch_done

# --- 2026-07-25 accounts (Sign in with Apple, Stage 1) ---
# The dev server's account storage. accounts.json holds Apple's per-app identifier for each
# player and sessions.json holds live session tokens, so none of these may ever reach this
# public repo. The Deno production server keeps the same six things in KV, not on disk.
server/accounts.json
server/account-index.json
server/sessions.json
server/auth-nonces.json
server/accounts-leaderboard.json
server/claims.json
# email-codes.json holds a HASH of each pending six-digit sign-in code plus the address it was
# sent to, so it is personal data even though the code itself is never written down.
server/email-codes.json
# The Sign in with Apple key, when Blake creates it (same treatment as the APNs key above).
server/apple-key.p8
server/apple-key-id.txt
# 2026-07-25: the other three sign-in methods' secrets, for the dev server only (production reads
# them from Deno Deploy environment variables). Same treatment as the APNs key: never committed,
# and the server no-ops gracefully when they are absent.
server/facebook-app-secret.txt
server/email-api-key.txt
